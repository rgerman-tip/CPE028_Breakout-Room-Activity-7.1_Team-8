#:include frontscreen.kv
#:include output.kv
#:include logs.kv
#:include recent.kv
#:import NoTransition kivy.uix.screenmanager.NoTransition

ScreenManager:
    transition: NoTransition()
    Front:
        id: front
    Output:
        id: output
    Logs:
        id: logs
    Recent:
        id: recent
